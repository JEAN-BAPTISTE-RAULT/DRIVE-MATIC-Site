{#
  SDC « Bloc :: Coordonnées de contact » — adresse/horaires (body) et visuel
  (field_photo, recadré 16:9) côte à côte au-dessus du formulaire.
  Maquettes 433-7637 / 438-9060 / 438-9465.
  Slots : text, image.
#}
<div class="contact-intro">
  <div class="contact-intro__text">{{ block('text') }}</div>
  <div class="contact-intro__media">{{ block('image') }}</div>
</div>
