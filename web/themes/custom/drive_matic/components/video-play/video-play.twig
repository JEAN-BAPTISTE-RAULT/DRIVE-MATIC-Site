{#
  SDC « Bouton :: Lecture vidéo » — plaque de lecture commune à TOUTES les
  façades vidéo (`video_centered`, `history_element`, `product_video_element`).
  Sans prop : purement présentationnel. À inclure dans le `<button>` de la
  façade — `{{ include('drive_matic:video-play') }}` — dont le conteneur doit
  être positionné (la plaque se centre en absolu sur la miniature).
#}
<span class="video-play" aria-hidden="true"></span>
